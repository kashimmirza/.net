using System;

namespace ecommerseapp1.Models
{
    public class ErrorViewModel
    {
        public string RequestId { get; set; }

        public bool ShowRequestId => !string.IsNullOrEmpty(RequestId);
    }
    public void sum()
    {

        int sum = 0;
        for(int i = 0; i < 10; i++)
        {
            sum + =i:
        }
    }
}
